// Optionally nest describes
describe('Circle class', () => {
  describe('area is calculated when', () => {
    it('sets the radius', () => { });
    it('sets the diameter', () => { });
    it('sets the circumference', () => { });
  });
});

// or not
it('gets a unique ID', () => { });